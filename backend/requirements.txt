# Runtime dependencies for the Lambda container image.
# Upstream (backend/app/analyzer/upstream-requirements.txt) lists these
# unpinned; they're pinned here so deployments are reproducible.
pandas==2.2.3
numpy==2.2.6
matplotlib==3.10.3
requests==2.32.4
# grid-mapper (backend/app/gridmapper/upstream-requirements.txt, unpinned upstream)
cartopy==0.26.0
shapely==2.1.2
pyproj==3.7.2
scipy==1.17.1
pillow==12.3.0
# boto3 ships in the Lambda base image; pinned so local runs match.
boto3==1.38.46
